
  # Qugenie prototype

  This is a code bundle for Qugenie prototype. The original project is available at https://www.figma.com/design/x98d4g40H3oItW6htPmoux/Qugenie-prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  